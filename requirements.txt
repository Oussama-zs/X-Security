# AI Analysis Agent - Requirements
# These are the direct dependencies needed to run the project.

# Used in ingestion.py for parsing .gitignore files
pathspec==1.1.1

# Used in ai_supervisor.py for sending HTTPS requests to LLMs
requests==2.34.2

# Used in reporter.py to generate PDF reports
fpdf2==2.8.7

# The static analysis engine used by static_analyzer.py
semgrep==1.170.1

# The dynamic analysis engine (DAST) used by dynamic_analyzer.py
wapiti3==3.3.1

# (Optional) Used for running the dummy vulnerable application
Flask==3.1.3

# (Optional) Used in md_to_pdf.py to generate the course PDF
Markdown==3.10.2
